defaults:
  - model: PriorVAE
  - kernel: SquaredExponential
  - grid: defaults
  - train: defaults
  - test: defaults
  - wandb: defaults
  - optimizer: defaults
  - dataset: defaults
  - test_runner: defaults
